<div class="error-message text-danger">
<% if model.errors.any? %>
  <p><%= model.errors.count %> つのエラーがあります。</p>
  <% model.errors.full_messages.each do |message| %>
    <p>・<%= message %></p>
  <% end %>
<% end %>
</div>